{
  "name": "tema-360vo-2026",
  "private": true,
  "scripts": {
    "build:assets": "node scripts/build-assets.mjs build",
    "watch:assets": "node scripts/build-assets.mjs watch",
    "setup:hooks": "node scripts/install-git-hooks.mjs",
    "prepare": "node scripts/install-git-hooks.mjs && npm run build:assets"
  }
}
